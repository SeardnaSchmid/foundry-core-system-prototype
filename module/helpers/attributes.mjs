// Attribute range from the "Attribut-Heatmap" spec. `base` is the sole
// persisted and rolled rating; XP advancement and manual correction both keep
// it inside this rulebook range.
export const BASE_MIN = 1;
export const BASE_MAX = 10;

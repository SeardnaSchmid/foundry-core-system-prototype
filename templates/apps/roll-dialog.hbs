<form class="edgefall-roll-dialog flexcol">
  <div class="edgefall-roll-subject">
    <h2 class="edgefall-roll-subject-name">{{subject}}</h2>
  </div>

  <div class="edgefall-roll-stats">
    {{#if isSkillMode}}
    <div class="form-group edgefall-attribute-field">
      <label>{{localize "EDGEFALL.Roll.ChooseAttribute"}}</label>
      <div class="edgefall-attribute-chip-grid">
        {{#each attributeGrid as |row|}}
        {{#each row as |chip|}}
        <a class="edgefall-attribute-chip {{#if chip.active}}active{{/if}}" data-key="{{chip.key}}" title="{{chip.label}}" style="background:{{chip.cellBg}}; color:{{chip.textColor}};">
          <span class="edgefall-attribute-chip-abbr">{{chip.abbr}}</span>
          <span class="edgefall-attribute-chip-value">{{chip.value}}</span>
        </a>
        {{/each}}
        {{/each}}
      </div>
      <input type="hidden" name="attributeA" value="{{attributeA}}"/>
    </div>
    <div class="edgefall-roll-detail">
      <span class="edgefall-roll-detail-label">{{skillLabel}}</span>
      <span class="edgefall-roll-detail-value">{{skillValue}}</span>
    </div>
    {{else if isFixedMode}}
    <div class="edgefall-roll-detail">
      <span class="edgefall-roll-detail-label">{{fixedLabel}}</span>
      <span class="edgefall-roll-detail-value">{{fixedValueDisplay}}</span>
    </div>
    {{else}}
    <div class="form-group">
      <label>{{localize "EDGEFALL.Roll.AttributeA"}}</label>
      <select name="attributeA">
        <option value="">-</option>
        {{#each abilities as |label key|}}
        <option value="{{key}}" {{#ifEquals key ../attributeA}}selected{{/ifEquals}}>{{label}}</option>
        {{/each}}
      </select>
    </div>

    {{#if isFreeMode}}
    <div class="form-group">
      <label>{{localize "EDGEFALL.Roll.FreeSkillValue"}}</label>
      <input type="number" name="skillValue" value="{{skillValue}}" min="0" max="10" step="1"/>
    </div>
    {{else}}
    <div class="form-group">
      <label>{{localize "EDGEFALL.Roll.AttributeB"}}</label>
      <select name="attributeB">
        <option value="">-</option>
        {{#each abilities as |label key|}}
        <option value="{{key}}" {{#ifEquals key ../attributeB}}selected{{/ifEquals}}>{{label}}</option>
        {{/each}}
      </select>
    </div>
    {{/if}}
    {{/if}}
  </div>

  {{#unless isFixedMode}}
  <hr class="edgefall-roll-divider"/>

  <div class="form-group">
    <label>{{localize "EDGEFALL.Roll.Bonus"}}</label>
    <div class="edgefall-bonus-stepper-group flexrow flex-center">
      <a class="edgefall-bonus-stepper" data-action="decrement">−</a>
      <span class="edgefall-bonus-value">{{bonus}}</span>
      <a class="edgefall-bonus-stepper" data-action="increment">+</a>
    </div>
    <input type="hidden" name="bonus" value="{{bonus}}"/>
  </div>

  <div class="form-group edgefall-advantage-field">
    <label>{{localize "EDGEFALL.Roll.Advantage"}}</label>
    <div class="edgefall-advantage-group flexrow flex-center">
      {{#each advantageOptions as |option|}}
      <a class="edgefall-advantage-option {{#ifEquals option.value ../advantage}}active{{/ifEquals}}" data-value="{{option.value}}" title="{{#if option.isDefault}}{{localize 'EDGEFALL.Roll.AdvantageDefaultHint' label=option.label}}{{else}}{{option.label}}{{/if}}">{{option.abbr}}</a>
      {{/each}}
    </div>
    <input type="hidden" name="advantage" value="{{advantage}}"/>
  </div>
  {{else}}
  <input type="hidden" name="bonus" value="{{bonus}}"/>
  <input type="hidden" name="advantage" value="{{advantage}}"/>
  {{/unless}}

  {{#if hasIdeaOption}}
  <div class="form-group edgefall-idea-field">
    <label class="edgefall-idea-label {{#if ideaDisabled}}edgefall-idea-disabled{{/if}}" title="{{#if ideaDisabled}}{{localize 'EDGEFALL.Notify.NoReserve'}}{{else}}{{localize 'EDGEFALL.DerivedHint.SolveIdea'}} — {{localize 'EDGEFALL.Notify.ProblemSolvingGate'}}{{/if}}">
      <input type="checkbox" name="useIdea" {{#if useIdea}}checked{{/if}} {{#if ideaDisabled}}disabled{{/if}}/>
      {{localize "EDGEFALL.Roll.IdeaToggle"}} (+{{solveIdeaValue}})
    </label>
  </div>
  {{/if}}

  <hr class="edgefall-roll-divider"/>

  <div class="edgefall-roll-threshold-preview">
    {{localize "EDGEFALL.Roll.Threshold"}}: &lt;= <span class="edgefall-threshold-value">{{threshold}}</span>
  </div>

  <footer class="sheet-footer flexrow">
    <button type="submit"><i class="fas fa-dice-d20"></i> {{localize "EDGEFALL.Roll.Submit"}}</button>
  </footer>
</form>

<div class="edgefall-roll-card dice-roll" data-action="expandRoll">
  <div class="edgefall-roll-summary">
    <div class="edgefall-roll-title">
      {{#if flavor}}
      <h3 class="edgefall-roll-flavor">{{flavor}}</h3>
      {{/if}}
      <span class="edgefall-roll-advantage-badge" title="{{advantageLabel}}">{{advantageAbbr}}</span>
    </div>

    <div class="edgefall-roll-dice">
      {{#each dice as |die|}}
      <span class="edgefall-die{{#if die.isCounted}} counted{{/if}}">{{die.value}}</span>
      {{/each}}
      {{#if hasThreshold}}
      <span class="edgefall-roll-threshold" title="{{localize "EDGEFALL.Roll.Threshold"}}">&le; {{threshold}}</span>
      {{/if}}
    </div>

    {{#if outcome}}
    <div class="edgefall-roll-outcome {{outcome}}">{{outcomeLabel}}</div>
    {{/if}}
  </div>

  <div class="dice-tooltip edgefall-roll-tooltip">
    <div class="wrapper">
      {{#each components as |component|}}
      <div class="edgefall-roll-detail">
        <span class="edgefall-roll-detail-label">{{component.label}}</span>
        <span class="edgefall-roll-detail-value">{{component.value}}</span>
      </div>
      {{/each}}
      {{#if showBonus}}
      <div class="edgefall-roll-detail">
        <span class="edgefall-roll-detail-label">{{localize "EDGEFALL.Roll.Bonus"}}</span>
        <span class="edgefall-roll-detail-value">{{bonusDisplay}}</span>
      </div>
      {{/if}}
      {{#if hasThreshold}}
      <div class="edgefall-roll-detail">
        <span class="edgefall-roll-detail-label">{{localize "EDGEFALL.Roll.Threshold"}}</span>
        <span class="edgefall-roll-detail-value">{{threshold}}</span>
      </div>
      {{/if}}
      <div class="edgefall-roll-detail">
        <span class="edgefall-roll-detail-label">{{localize "EDGEFALL.Roll.Advantage"}}</span>
        <span class="edgefall-roll-detail-value">{{advantageLabel}}</span>
      </div>
    </div>
  </div>
</div>
